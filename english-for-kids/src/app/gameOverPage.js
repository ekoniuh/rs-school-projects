function sdf(result) {
  const fragment = document.createDocumentFragment();

  const smile = document.createElement('img');
  smile.classList.add();
  smile.setAttribute('src', './....');
  smile.setAttribute('alt', './....');

  fragment.append(smile);
  return fragment;
}

