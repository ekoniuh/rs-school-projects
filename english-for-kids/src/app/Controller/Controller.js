export class Controller2 {}
model view

cons{
	this.model = model;
	this.view = view;
}

Controller.on('cardClick', () => {
	model.checkCard правильно ли выбрали
	
})
Controller.on('cardClick', () => {
	model.checkCard правильно ли выбрали 
	
})
Controller.on('cardClick', () => {
	model.checkCard правильно ли выбрали
	
})
